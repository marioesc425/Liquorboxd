services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: liquorboxd
      POSTGRES_USER: liquorboxd_user
      POSTGRES_PASSWORD: changeme123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: